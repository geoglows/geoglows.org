---
import GeoGlyph from "../site/GeoGlyph.astro";

interface Props {
  eyebrow?: string;
  heading: string;
  intro?: string;
  align?: "center" | "left";
  level?: 1 | 2;
}
const { eyebrow, heading, intro, align = "center", level = 2 } = Astro.props;
const Heading = `h${level}` as "h1" | "h2";
---

<div class:list={["section-header", align]}>
  {
    eyebrow && (
      <p class="eyebrow">
        <GeoGlyph />
        {eyebrow}
      </p>
    )
  }
  <Heading>{heading}</Heading>
  {intro && <p class="intro">{intro}</p>}
</div>

<style>
  .section-header {
    max-width: 760px;
    margin-bottom: 44px;
  }
  .section-header.center {
    margin-inline: auto;
    text-align: center;
  }
  .section-header.center .eyebrow {
    justify-content: center;
  }
  h1,
  h2 {
    font-size: clamp(1.6rem, 2.6vw, 2.3rem);
    letter-spacing: -0.02em;
    line-height: 1.15;
  }
  .intro {
    /* Measured at 83ch before this cap, well past the 65-75ch a reader can
       track back to the start of the next line. */
    max-width: 70ch;
    margin-top: 14px;
    color: var(--text-body);
    font-size: 1rem;
    line-height: 1.6;
  }
  .center .intro {
    margin-inline: auto;
  }
</style>
